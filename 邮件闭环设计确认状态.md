# 邮件闭环设计确认状态

## 当前状态

设计边界已经明确：

- 会计发邮件和附件作为输入源。
- 系统解析 Excel 或 CSV 附件。
- 系统自动识别研发费用并更新台账。
- 不确定记录交给项目负责人审核。
- CEO/CFO 只收到简版 HTML 预警简报。

现在还没有进入正式开发。

原因是：邮箱、授权密码、收件人、样表字段这些业务前置条件还需要确认。

## 已经确定的设计原则

- 不把复杂明细塞进管理层页面。
- 管理层只看预警和关键数字。
- 明细账解析、费用口径、项目匹配、人员分摊都放后台。
- 第一版只做 Excel 和 CSV。
- 第一版不接完整会计系统 API。
- 邮箱密码不能写进网页文件，也不能放 GitHub。

## 推荐默认值

- 专用邮箱：`subsidy-ledger@synbiome.cn`
- 发送时间：每月 6 日上午 9 点
- 审核人：张英
- 附件格式：Excel 或 CSV
- 管理层简报：只显示预警等级、研发投入达标率、研发投入缺口、补贴到账率、三条行动建议

## 还需要确认

1. 是否创建专用邮箱 `subsidy-ledger@synbiome.cn`。
2. 是否能拿到专用邮箱授权密码。
3. 月报最终收件人名单。
4. 会计能否导出一份真实样表。
5. 样表是否包含日期、主体、科目、摘要、金额、凭证号、部门或项目。
6. 是否同意张英作为不确定记录审核人。
7. 是否同意每月 6 日上午 9 点发送。

## 相关文件

- `邮件闭环资料总索引.md`：按管理层、会计、张英、IT 和实施人员分发资料。
- `邮件闭环设计确认单.md`：给业务负责人勾选确认。
- `邮件闭环决策一页纸.md`：给负责人快速拍板。
- `邮件闭环实施计划.md`：给管理层和操作人员看的简版说明。
- `邮件驱动闭环设计说明.md`：说明整体设计边界。
- `会计研发费用明细样表.csv`：给会计对照导出字段。
- `附件字段映射确认表.csv`：给会计确认真实账表列名如何对应系统字段。
- `会计样表填写说明.md`：说明每一列怎么填。
- `测试邮件模板.md`：给会计发送测试邮件时直接套用。
- `管理层月报样张.html`：给 CEO/CFO 确认最终收到的简报长什么样。
- `邮箱授权安全交接说明.md`：给邮箱管理员或 IT 安全配置授权密码。
- `邮件闭环上线前检查清单.md`：正式开发或上线前逐项检查。
- `邮件闭环三方确认回执表.csv`：让管理层、会计、张英和 IT 分别确认自己负责的事项。
- `邮件闭环测试验收流程.md`：正式开发完成后按步骤验收。
- `邮件闭环业务确认跟踪表.csv`：跟踪邮箱、样表、收件人、审核人等确认状态。
- `发给会计和负责人的通知草稿.md`：可直接发给会计和张英收集样表。
- `docs/superpowers/specs/2026-06-20-mail-driven-ledger-design.md`：正式设计规格书。
- `docs/superpowers/plans/2026-06-20-mail-driven-ledger.md`：后续开发实施计划。

## 下一步

拿到以下三样东西后，可以进入正式开发：

1. 专用邮箱和授权密码。
2. 一份真实的会计导出样表。
3. 业务负责人确认后的 `邮件闭环设计确认单.md`。
4. 邮箱管理员按 `邮箱授权安全交接说明.md` 配置环境变量。
5. 三方确认回执和测试验收流程准备进入执行。
